Arquivos legados isolados.

Estes arquivos nao fazem parte do fluxo atual de filas/workers. Foram movidos
para ca para evitar import acidental e preservar referencia historica enquanto
o sistema usa `src/queues/jobQueue.js` e `src/workers/queueWorker.js`.
